import { DoctorPrescription } from './doctor-prescription';

describe('DoctorPrescription', () => {
  it('should create an instance', () => {
    expect(new DoctorPrescription()).toBeTruthy();
  });
});
