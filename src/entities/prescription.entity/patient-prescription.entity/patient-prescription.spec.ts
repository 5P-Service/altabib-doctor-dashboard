import { PatientPrescription } from './patient-prescription';

describe('PatientPrescription', () => {
  it('should create an instance', () => {
    expect(new PatientPrescription()).toBeTruthy();
  });
});
