import { CabinetOrganisation } from './cabinet-organisation';

describe('CabinetOrganisation', () => {
  it('should create an instance', () => {
    expect(new CabinetOrganisation()).toBeTruthy();
  });
});
